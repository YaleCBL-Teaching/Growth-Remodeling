% ============================================================================
%  Growth & Remodeling seminar — shared beamer preamble
%  Adapted from the YaleCBL-Teaching/BENG_5570 slide build system.
%  Two build modes are selected by \teachertrue / \teacherfalse in the
%  master files (slides-student.tex / slides-teacher.tex):
%    student  -> key equations shown as EMPTY numbered boxes to fill on iPad
%    teacher  -> the same boxes are filled with the actual equation
% ============================================================================
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts,mathtools}
\usepackage{bm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc}
\usepackage[export]{adjustbox}
\usepackage{xcolor}

% ---- sans-serif math+text (matches BENG_5570 house style) ------------------
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usefonttheme{professionalfonts}

% ---- Yale-blue palette -----------------------------------------------------
\definecolor{YaleBlue}{RGB}{0,53,107}
\definecolor{YaleLightBlue}{RGB}{40,109,192}
\definecolor{AccentRed}{RGB}{171,38,38}
\definecolor{BoxGray}{RGB}{120,120,120}
\definecolor{SlideFrame}{RGB}{0,53,107}

% ---- beamer theme ----------------------------------------------------------
\usetheme{default}
\usecolortheme{default}
\setbeamercolor{frametitle}{fg=YaleBlue}
\setbeamercolor{title}{fg=YaleBlue}
\setbeamercolor{structure}{fg=YaleLightBlue}
\setbeamercolor{item}{fg=YaleBlue}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{itemize items}[circle]
\setbeamerfont{frametitle}{series=\bfseries}
\setbeamersize{text margin left=1.2em,text margin right=1.2em}

% footline: course tag + slide number
% Lower-left carries the slide's reference (set via \srccite), not a running
% title; lower-right the frame number. The stored reference resets each shipout.
\setbeamertemplate{footline}{%
  \leavevmode\hbox{%
    \begin{beamercolorbox}[wd=\paperwidth,ht=1.6ex,dp=3.8ex,leftskip=1.2em,rightskip=1.2em]{}%
      {\scriptsize\color{BoxGray}%
        \begin{minipage}[t]{\dimexpr\paperwidth-5.2em\relax}\itshape\sliderefstore\end{minipage}%
        \hfill
        \begin{minipage}[t]{1.6em}\raggedleft\upshape\insertframenumber\end{minipage}}%
    \end{beamercolorbox}}%
  \gdef\sliderefstore{}%
}

% ============================================================================
%  Fill-in-the-blank equation mechanism
% ============================================================================
% \teacher toggle set in the master file.
\newif\ifteacher

% Height register for a student blank box.
% Usage in slides:
%   \slboxeq{<box height>}{<eq label>}{<equation body>}
%     student : empty framed box of the given height, right-numbered (label)
%     teacher : the equation, boxed and numbered (same label)
% The label is shared, so \eqref works identically in both builds.
\newcommand{\slboxeq}[3]{%
  \ifteacher
    % teacher: show the boxed, numbered equation
    \begin{equation}\label{#2}
      \boxed{\displaystyle #3}
    \end{equation}
  \else
    % student: reserve an empty framed box carrying the equation number
    \begin{equation}\label{#2}\end{equation}%
    \vspace{-2.2\baselineskip}%
    \begin{center}%
    \setlength{\fboxrule}{0.8pt}\setlength{\fboxsep}{4pt}%
    \textcolor{SlideFrame}{\framebox[0.92\linewidth][c]{%
      \rule{0pt}{#1}\rule{0pt}{0pt}%
    }}%
    \end{center}%
    \vspace{-0.3\baselineskip}%
  \fi
}

% Inline "given" equation (always shown in both builds, unnumbered box-free).
\newcommand{\giveneq}[1]{\begin{equation*}#1\end{equation*}}

% ============================================================================
%  3D <-> 0D presentation convention
%  Every core relation is shown first in its general 3D (tensor) form, then in
%  the 0D scalar reduction actually used in this course. A small colored badge
%  marks which regime a line belongs to; \formlab places the badge + a short
%  caption on a tight line just above the equation it introduces.
% ============================================================================
\definecolor{TagD}{RGB}{62,78,138}    % indigo/slate  -> 3D (general tensor)
\definecolor{TagZ}{RGB}{17,122,116}   % teal          -> 0D (scalar reduction)
\newcommand{\dpill}[2]{\tikz[baseline]{%
  \node[fill=#1,text=white,rounded corners=2.5pt,inner xsep=4pt,inner ysep=1.6pt,
        font=\bfseries\scriptsize,anchor=base]{#2};}}
\newcommand{\tagd}{\dpill{TagD}{3D}}
\newcommand{\tagz}{\dpill{TagZ}{0D}}
% \formlab{<badge>}{<short caption>} — tight labeled line before an equation
\newcommand{\formlab}[2]{\par\smallskip\noindent
  {#1}\;{\footnotesize\color{black!62}#2}\par\nobreak\vspace{-0.2em}}
% convenience wrappers
\newcommand{\dlab}[1]{\formlab{\tagd}{#1}}   % 3D general (tensor) form
\newcommand{\zlab}[1]{\formlab{\tagz}{#1}}   % 0D scalar reduction

% ---- per-slide source citation, required format ----------------------------
% "First, ..., Last, short journal (year)"
% The reference is not printed in the slide body; it is stashed and shown in the
% lower-left footline (see the footline template above). Reset each shipout.
\newcommand{\sliderefstore}{}
\newcommand{\srccite}[1]{\gdef\sliderefstore{#1}}
% bottom-right floating credit for a figure
\newcommand{\figcredit}[1]{%
  \begin{flushright}\vspace{-0.6em}{\tiny\color{BoxGray} #1}\end{flushright}%
}

% ---- convenience figure command --------------------------------------------
\newcommand{\slidefig}[2][0.8\linewidth]{%
  \begin{center}\includegraphics[width=#1,height=0.62\textheight,keepaspectratio]{figures/#2}\end{center}%
}

% ---- big-figure slide: one large centred figure + short caption ------------
% \bigfig{file}{one-line caption}  — figure dominates the slide, minimal text.
% Captions under figures are intentionally suppressed (#2/#3 ignored).
\newcommand{\bigfig}[2]{%
  \vfill
  \begin{center}%
    \includegraphics[width=0.92\linewidth,height=0.66\textheight,keepaspectratio]{figures/#1}%
  \end{center}%
  \vfill
}
% variant with adjustable width for tall figures
\newcommand{\bigfigw}[3]{%
  \vfill
  \begin{center}%
    \includegraphics[width=#2,height=0.66\textheight,keepaspectratio]{figures/#1}%
  \end{center}%
  \vfill
}

% tall variant: figure fills the available vertical space (future-perspectives)
\newcommand{\bigfigtall}[2]{%
  \vfill
  \begin{center}%
    \includegraphics[width=#2,height=0.82\textheight,keepaspectratio]{figures/#1}%
  \end{center}%
  \vfill
}

% ---- math shorthands used across the deck ----------------------------------
\newcommand{\dd}{\mathrm{d}}
\newcommand{\Ftens}{\bm{F}}
\newcommand{\sig}{\sigma}
\newcommand{\sigbar}{\bar{\sigma}}
\newcommand{\sigh}{\bar{\sigma}_h}
\newcommand{\lam}{\lambda}
\newcommand{\Ups}{\Upsilon}
\newcommand{\kd}{k_d}

% ============================================================================
%  Interactive-exercise format
%  A standard "Your turn" question frame (Question / Resources / Hints) and a
%  matching worked "Answer" frame. Used throughout so there is always something
%  to do every few slides.
% ============================================================================
\usepackage{tcolorbox}
\usepackage{fontawesome5}
\tcbuselibrary{skins,breakable}
\definecolor{QBlue}{RGB}{40,109,192}
\definecolor{ABlue}{RGB}{22,120,70}   % green accent for answers
\definecolor{QFill}{RGB}{236,242,250}
\definecolor{AFill}{RGB}{234,246,238}

% \begin{yourturn}{<short title>} ... \fields ... \end{yourturn}
\newtcolorbox{yourturnbox}[1]{%
  enhanced, breakable, colback=QFill, colframe=QBlue, boxrule=1pt,
  arc=2pt, left=6pt,right=6pt,top=4pt,bottom=4pt,
  fonttitle=\bfseries\color{white},
  title={\faPencilRuler~~Your turn: #1}}
\newtcolorbox{answerbox}[1]{%
  enhanced, breakable, colback=AFill, colframe=ABlue, boxrule=1pt,
  arc=2pt, left=6pt,right=6pt,top=4pt,bottom=4pt,
  fonttitle=\bfseries\color{white},
  title={\faCheckCircle~~Answer: #1}}

% field labels inside a yourturn box
\newcommand{\qq}[1]{\par\smallskip{\color{QBlue}\bfseries Question.}\ #1}
\newcommand{\qres}[1]{\par\smallskip{\color{QBlue}\bfseries Resources.}\ #1}
\newcommand{\qhint}[1]{\par\smallskip{\color{QBlue}\bfseries Hints.}\ #1}
\newcommand{\qkind}[1]{}

% ---- problem-setup box (precedes each coding exercise) ---------------------
% A neutral slate box laying out the problem in enough detail to implement it
% from scratch, plus a short note on how to drive the gr package.
\definecolor{SFrame}{RGB}{62,78,138}     % slate/indigo (matches the 3D badge)
\definecolor{SFill}{RGB}{243,244,250}
\newtcolorbox{setupbox}[1]{%
  enhanced, breakable, colback=SFill, colframe=SFrame, boxrule=1pt,
  arc=2pt, left=6pt,right=6pt,top=4pt,bottom=4pt,
  fonttitle=\bfseries\color{white},
  title={\faCogs~~Setup: #1}}
% field labels inside a setup box.  The general problem setup lives on its own
% frame (the "gr toolbox"); each exercise box is then lean: the specific example
% and how to run it, with an optional parametric-study line.
\newcommand{\sprob}[1]{\par\smallskip{\color{SFrame}\bfseries Example.}\ #1}
\newcommand{\srun}[1]{\par\smallskip{\color{SFrame}\bfseries Run it.}\ #1}
\newcommand{\sstudy}[1]{\par\smallskip{\color{SFrame}\bfseries Parametric study.}\ #1}
% one code line in monospace inside a setup box
\newcommand{\scodeline}[1]{{\ttfamily\small #1}\par}


% ===========================================================================
%  Idealized blood vessel drawn in TikZ (thick-walled cylinder cross-section)
%    \idealvessel        -> full annotated version (P, Q, r, h, sigma_theta, tau_w)
% ============================================================================
% Idealized vessel as a LONGITUDINAL CUT (pipe sliced along its axis):
%   radial quantities (a, h, P) read vertically; axial (Q, tau_w) horizontally.
% ---- homeostatic G&R feedback loop (TikZ) \gnrloop -------------------------
% Applied hemodynamic load -> biomechanical state (regulated sigma, tau_w) ->
% homeostatic checks (set-points vs stimuli) -> evolving geometry/composition
% -> single return arrow back into the applied load. (Redraw of the slide-6
% schematic cited to Humphrey, J Elasticity (2021); left-hand fluid/FSI/solid
% detail and the this-work/prior-work split omitted per feedback.)
\newcommand{\gnrloop}[1][1.0]{%
\begin{tikzpicture}[scale=#1,>={Latex[length=2.2mm]},
    box/.style={draw=YaleBlue,very thick,rounded corners=2pt,fill=YaleLightBlue!12,
      align=center,inner sep=6pt,font=\small,text width=6.4cm,minimum height=0.95cm},
    flow/.style={-{Latex[length=2.4mm]},very thick,black!75}]
  \node[box] (load)  at (0,4.5)  {$\mathcal{F}$: applied hemodynamic load\\ \footnotesize (pressure $P$, flow $Q$)};
  \node[box] (state) at (0,3.0)  {biomechanical state\\ \footnotesize regulated variables $\sigma_\theta,\ \tau_w$};
  \node[box] (check) at (0,1.5)  {homeostatic checks\\ \footnotesize set-points vs.\ stimuli: $\Delta\sigma_\theta,\ \Delta\tau_w$};
  \node[box] (evolve) at (0,0.0) {$\mathcal{S}$: evolving geometry,\\ composition, properties};
  \draw[flow] (load)  -- (state);
  \draw[flow] (state) -- (check);
  \draw[flow] (check) -- (evolve);
  % single return arrow: adaptation changes the load the tissue sees
  \draw[flow] (evolve.east) -- ++(4.2,0) |- (load.east);
\end{tikzpicture}}

\newcommand{\idealvessel}[1][1.0]{%
\begin{tikzpicture}[scale=#1,>={Latex[length=1.8mm]}]
  \def\L{8.4}      % axial length
  \def\a{1.9}      % inner radius (axis -> inner wall)
  \def\h{0.85}     % wall thickness
  % --- shaded lumen (blood-filled interior) ---
  \fill[AccentRed!7] (0,-\a) rectangle (\L,\a);
  % --- wall bands (top & bottom) ---
  \fill[YaleLightBlue!22] (0,\a) rectangle (\L,\a+\h);
  \fill[YaleLightBlue!22] (0,-\a) rectangle (\L,-\a-\h);
  \draw[YaleBlue,very thick] (0,\a) rectangle (\L,\a+\h);
  \draw[YaleBlue,very thick] (0,-\a) rectangle (\L,-\a-\h);
  % --- axis ---
  \draw[dash dot,black!55] (-0.2,0) -- (\L+0.4,0);
  % --- blood pressure P: pushes outward on both inner surfaces ---
  \foreach \x in {1.9,3.0,4.1}{
    \draw[->,YaleLightBlue,thick] (\x,0.5) -- (\x,\a-0.06);
    \draw[->,YaleLightBlue,thick] (\x,-0.5) -- (\x,-\a+0.06);}
  \node[YaleLightBlue] at (3.0,0.0) {\small $P$};
  % --- flow Q along the axis ---
  \draw[->,AccentRed,very thick] (5.6,0) -- (7.7,0) node[midway,above=2pt]{\small $Q$};
  % --- wall shear stress tau_w on BOTH inner surfaces ---
  \draw[->,AccentRed] (5.8,\a-0.24) -- (7.3,\a-0.24)
       node[midway,above=0pt]{\scriptsize $\tau_w$};
  \draw[->,AccentRed] (5.8,-\a+0.24) -- (7.3,-\a+0.24)
       node[midway,below=0pt]{\scriptsize $\tau_w$};
  % --- circumferential (hoop) stress sigma_theta carried by the wall ---
  \draw[<->,YaleBlue,thick] (0.9,\a+\h/2) -- (3.4,\a+\h/2);
  \node[YaleBlue] at (2.15,\a+\h/2+0.36) {\scriptsize $\sigma_\theta$ (hoop)};
  % --- radius a (axis -> inner wall) ---
  \draw[<->,thick] (0.7,0) -- (0.7,\a) node[midway,left]{\small $a$};
  \fill (0.7,0) circle (1.4pt);
  % --- wall thickness h ---
  \draw[<->,thick,AccentRed] (\L+0.25,\a) -- (\L+0.25,\a+\h)
       node[midway,right]{\small $h$};
\end{tikzpicture}}

% ============================================================================
%  Reference -> current configuration mapping (TikZ)  \configmap
% ============================================================================
% ============================================================================
%  Configuration-mapping diagrams (TikZ, grayscale)
%    \configmap  reference -> current (deformation gradient F)
%    \kinmap     kinematic growth multiplicative split  F = F_e F_g
%    \cmmmap     constrained mixture: cohorts, each its own natural config
%  Shared style: stress-free/reference bodies light gray, loaded/current
%  bodies darker gray; labels placed clear of the blobs to avoid overlap.
% ============================================================================
\tikzset{
  refbody/.style={draw=black!70,very thick,fill=black!8},
  intbody/.style={draw=black!70,very thick,fill=black!16},
  curbody/.style={draw=black!80,very thick,fill=black!26},
  cfgmaparrow/.style={-{Latex[length=2.2mm]},thick,black!75},
  cfgbase/.style={-{Latex[length=1.2mm]},black!60},
}

% ---- reference -> current --------------------------------------------------
\newcommand{\configmap}[1][1.0]{%
\begin{tikzpicture}[scale=#1,every node/.style={font=\small}]
  % reference body (left)
  \draw[refbody] plot[smooth cycle,tension=0.9]
    coordinates {(0,0)(1.0,0.55)(1.8,0.15)(1.55,-0.85)(0.55,-1.05)(-0.15,-0.5)};
  \fill (0.7,-0.35) circle (1.4pt);
  \draw[-{Latex[length=1.6mm]},thick] (0.7,-0.35)--(1.15,-0.6)
    node[anchor=north west,font=\footnotesize,inner sep=1pt]{$\dd\bm{X}$};
  \node[font=\footnotesize] at (0.8,-1.55) {reference $\mathcal{B}_0$};
  % current body (right)
  \begin{scope}[shift={(5.2,-0.05)}]
    \draw[curbody] plot[smooth cycle,tension=0.9]
      coordinates {(0,0.2)(1.25,0.7)(2.25,0.2)(2.1,-1.0)(1.0,-1.45)(-0.1,-0.75)};
    \fill (1.05,-0.45) circle (1.4pt);
    \draw[-{Latex[length=1.6mm]},thick] (1.05,-0.45)--(1.6,-0.75)
      node[anchor=north west,font=\footnotesize,inner sep=1pt]{$\dd\bm{x}$};
    \node[font=\footnotesize] at (1.05,-2.0) {current $\mathcal{B}_t$};
  \end{scope}
  % motion (top arc, well above both bodies)
  \draw[cfgmaparrow] (2.0,0.85) to[bend left=20]
    node[above,font=\small,pos=0.5]{$\bm{\varphi}(t,\bm{X})$} (5.3,1.05);
  % line-element mapping (bottom arc, label below the arc, clear of bodies)
  \draw[cfgmaparrow] (1.9,-1.15) to[bend right=16] (6.15,-1.35);
  \node[font=\footnotesize] at (4.0,-1.95) {$\dd\bm{x}=\bm{F}\,\dd\bm{X}$};
\end{tikzpicture}}

% ---- kinematic growth: F = F_e F_g, with INCOMPATIBLE grown configuration ---
% Reference body is cut into pieces; each piece grows stress-free on its own, so
% the grown pieces no longer tile (overlaps + gaps = incompatible). The elastic
% step F_e forces them back into one connected body (residual stress).
\newcommand{\kinmap}[1][1.0]{%
\begin{tikzpicture}[scale=#1,every node/.style={font=\small},
    piece/.style={draw=black!70,thick,fill=black!10},
    gpiece/.style={draw=black!70,thick,dashed,fill=black!16},
    cpiece/.style={draw=black!80,thick,fill=black!24}]
  % --- reference B_0: 2x2 compatible tiling (left) ---
  \begin{scope}[shift={(0,0)}]
    \foreach \x in {0,0.9} \foreach \y in {-0.9,0}{
      \draw[piece] (\x,\y) rectangle ++(0.9,0.9);}
    \node[font=\footnotesize] at (0.9,-1.35) {reference $\mathcal{B}_0$};
  \end{scope}
  % --- grown, stress-free, INCOMPATIBLE (middle): each piece enlarged about
  %     its own centre + jittered so they overlap and leave gaps ---
  \begin{scope}[shift={(3.7,0.05)}]
    \draw[gpiece] (-0.12,0.02) rectangle ++(1.14,1.14);   % top-left, shifted
    \draw[gpiece] (1.02,0.12)  rectangle ++(1.14,1.14);   % top-right, overlaps
    \draw[gpiece] (0.06,-1.18) rectangle ++(1.14,1.14);   % bot-left
    \draw[gpiece] (1.16,-1.08) rectangle ++(1.14,1.14);   % bot-right, gap
    \node[font=\footnotesize,align=center] at (1.05,-1.7)
      {grown, stress-free\\\emph{incompatible} $\mathcal{B}_g$};
  \end{scope}
  % --- current, loaded, COMPATIBLE (right): reassembled connected body ---
  \begin{scope}[shift={(7.6,0)}]
    \foreach \x in {0,1.02} \foreach \y in {-1.02,0}{
      \draw[cpiece] (\x,\y) rectangle ++(1.02,1.02);}
    \node[font=\footnotesize] at (1.02,-1.42) {current $\mathcal{B}_t$};
  \end{scope}
  % --- arrows ---
  % F_g arrow low, label BELOW the arc in the clear gap (no piece overlap)
  \draw[cfgmaparrow] (2.0,-0.45) to[bend right=10] (3.55,-0.55);
  \node[font=\small,anchor=north] at (2.85,-0.75) {$\bm{F}_g$ (growth, prescribed)};
  % F_e arrow from high above, label clear at top-right
  \node[font=\small,anchor=south] at (7.0,2.15) {$\bm{F}_e$ (elastic, restores fit)};
  \draw[cfgmaparrow] (6.45,1.95) to[bend left=15] (7.7,0.7);
  \draw[cfgmaparrow,black!55] (0.5,-2.55) to[bend right=8]
    node[below,font=\footnotesize,pos=0.5]{$\bm{F}=\bm{F}_e\,\bm{F}_g$} (8.6,-2.35);
\end{tikzpicture}}

% ---- homogenized CMM: ONE evolving mean natural config per constituent --------
% Three tissue configurations along the bottom (reference -> initial -> current);
% above each, a SINGLE mean stress-free natural configuration per constituent
% (contrast with the full-history cohort stack of the constrained mixture).
\newcommand{\homogmap}[1][1.0]{%
\begin{tikzpicture}[scale=#1,every node/.style={font=\footnotesize},
    natA/.style={draw=black!70,thick,fill=black!10},
    natB/.style={draw=black!70,thick,fill=black!20},
    tisarr/.style={-{Latex[length=2.2mm]},thick,black!75},
    natarr/.style={-{Latex[length=1.8mm]},thick,black!60}]
  % --- three tissue bodies along the bottom ---
  \begin{scope}[shift={(0,0)}]
    \draw[refbody] plot[smooth cycle,tension=0.9]
      coordinates {(0,0)(0.85,0.4)(1.5,0)(1.25,-0.9)(0.4,-1.0)(-0.2,-0.5)};
    \node at (0.6,-1.4) {reference};
  \end{scope}
  \begin{scope}[shift={(4.0,0)}]
    \draw[intbody] plot[smooth cycle,tension=0.9]
      coordinates {(0,0.1)(0.95,0.5)(1.7,0.05)(1.45,-1.0)(0.5,-1.15)(-0.15,-0.6)};
    \node at (0.75,-1.55) {initial};
  \end{scope}
  \begin{scope}[shift={(8.2,0)}]
    \draw[curbody] plot[smooth cycle,tension=0.9]
      coordinates {(0,0.15)(1.15,0.65)(2.05,0.1)(1.85,-1.15)(0.7,-1.4)(-0.15,-0.7)};
    \node at (0.9,-1.8) {current};
  \end{scope}
  % --- tissue-level deformation arrows (bottom) ---
  \draw[tisarr] (1.7,-0.5)  to[bend right=8] node[below]{$\bm{F}(0)$} (3.7,-0.6);
  \draw[tisarr] (5.9,-0.6)  to[bend right=8] node[below]{$\bm{F}(s)$} (7.9,-0.75);
  % --- single mean natural config per constituent, ABOVE, evolving ---
  \foreach \sx/\sty in {0.35/natA, 4.35/natA, 8.55/natA}{
    \draw[\sty] (\sx,2.0) rectangle ++(0.62,0.62);}
  \foreach \sx/\sty in {1.05/natB, 5.05/natB, 9.25/natB}{
    \draw[\sty] (\sx,2.0) rectangle ++(0.62,0.62);}
  \node[align=center] at (1.0,3.05)
    {stress-free \emph{mean} natural configurations\\(one per constituent --- \emph{evolving}, not a history)};
  % arrows: single evolving natural config marches left->right
  \draw[natarr] (1.75,2.3) -- node[above]{$\bm{F}_n^k$} (4.3,2.3);
  \draw[natarr] (5.75,2.3) -- node[above]{$\bm{F}_n^k$} (8.5,2.3);
  % tie each mean config down to its tissue body
  \foreach \bx in {0.7,4.7,8.9}{
    \draw[natarr,black!45,dashed] (\bx,1.98) -- (\bx,0.75);}
\end{tikzpicture}}

% ---- Humphrey (2021) Fig. 1: finite deformations in a constrained mixture -----
% Horizontal barrel of stacked ellipse cross-sections = tissue evolving from
% kappa(0) -> kappa(tau) -> kappa(s); a deposited constituent sits at its own
% stress-free natural config kappa_n^alpha(tau) above kappa(tau), carried in by
% the deposition stretch G^alpha(tau); F_{n(tau)}^alpha(s) is the constituent-
% specific deformation; F(tau), F(s) are the tissue-level deformations.
\newcommand{\humphreygr}[1][1.0]{%
\begin{tikzpicture}[scale=#1,every node/.style={font=\small},
    ell/.style={draw=black!65,thick,fill=black!12},
    ellface/.style={draw=black!75,very thick,fill=black!18},
    grarr/.style={-{Latex[length=2.4mm]},very thick,black!78},
    tisarr/.style={-{Latex[length=2.2mm]},thick,black!70}]
  % Three labeled cross-sections at x = 0.6 (kappa 0), 5.0 (kappa tau), 9.4
  % (kappa s); the barrel ellipses GROW gradually left->right (ry ramps up).
  \def\xzero{0.6}\def\xtau{5.0}\def\xs{9.4}
  \def\rx{0.30}
  \pgfmathsetmacro\Nell{18}
  % --- barrel: faint ellipses, vertical radius grows CLEARLY left->right ---
  \foreach \i in {0,1,...,18}{
    \pgfmathsetmacro\xx{\xzero+\i*(\xs-\xzero)/\Nell}
    \pgfmathsetmacro\ryy{0.50+\i*0.055}   % visibly growing cross-section
    \draw[ell,fill opacity=0.85] (\xx,0) ellipse [x radius=\rx, y radius=\ryy];}
  % --- three labeled cross-sections (grown-face ellipses) ---
  \draw[ellface] (\xzero,0) ellipse [x radius=\rx, y radius=0.50];
  \draw[ellface] (\xtau,0)  ellipse [x radius=\rx, y radius={0.50+9*0.055}];
  \draw[ellface] (\xs,0)    ellipse [x radius=\rx, y radius={0.50+18*0.055}];
  \node[font=\footnotesize] at (\xzero,0) {$\kappa(0)$};
  \node[font=\footnotesize] at (\xtau,0)  {$\kappa(\tau)$};
  \node[font=\footnotesize] at (\xs,0)    {$\kappa(s)$};
  % dash-dot centre axis extending both ends
  \draw[densely dashdotted,black!55] (-0.4,0)--(\xzero-0.32,0);
  \draw[densely dashdotted,black!55] (\xs+0.32,0)--(\xs+1.0,0);
  \node[align=center,font=\footnotesize,black!75] at (\xs+1.1,-0.7)
    {Growth \&\\Remodeling};
  % --- deposited constituent at its stress-free natural config (top) ---
  % Outlined constituent dot sits AT THE START of the G^alpha(tau) arrow.
  \coordinate (kn) at (4.3,2.2);
  \node[circle,draw=black!75,thick,fill=black!22,minimum size=7pt,inner sep=0pt] at (kn) {};
  \node[anchor=south east,font=\footnotesize] at (4.18,2.28) {$\kappa_n^{\alpha}(\tau)$};
  % two more small constituent dots, EACH with its own arrow into kappa(tau)
  \fill[black!22,draw=black!70] (4.9,2.35) circle (2.6pt);
  \fill[black!22,draw=black!70] (5.5,2.25) circle (2.4pt);
  \draw[grarr] (4.9,2.14) to[bend left=12] (5.02,0.95);
  \draw[grarr] (5.5,2.04) to[bend left=10] (5.12,0.95);
  % deposition stretch G^alpha(tau): FROM the outlined dot down into kappa(tau)
  \draw[grarr] (kn) to[bend right=18]
    node[left,font=\small,pos=0.5]{$\bm{G}^{\alpha}(\tau)$} (4.9,0.95);
  % constituent-specific deformation F_{n(tau)}^alpha(s): natural config -> kappa(s)
  \draw[grarr] (4.55,2.3) to[bend left=18]
    node[above,font=\small,pos=0.55]{$\bm{F}^{\alpha}_{n(\tau)}(s)$} (9.15,1.4);
  % --- tissue-level deformations underneath (clear start/end at the ellipses) ---
  \draw[tisarr] (\xzero+0.35,-0.85) to[bend right=18]
    node[below,font=\small,pos=0.5]{$\bm{F}(\tau)$} (\xtau-0.35,-1.05);
  \draw[tisarr] (\xzero+0.35,-1.5) to[bend right=16]
    node[below,font=\small,pos=0.5]{$\bm{F}(s)$} (\xs-0.35,-1.75);
\end{tikzpicture}}

% ---- Cyron (2016) Fig. 2 (right): mechanical analog for mass turnover ---------
% A motor element (circle) exerting the prestress sigma_pre in PARALLEL with a
% Maxwell material = dashpot (time constant rho-dot0/rho0 + 1/T) in SERIES with a
% (nonlinear) spring of stiffness C. Load sigma applied top and bottom.
% HORIZONTAL Maxwell-fluid-with-a-motor analog (Cyron 2016, Fig. 2 right).
%   Load sigma acts left<->right. TOP branch = Maxwell arm: dashpot in SERIES
%   with a spring. BOTTOM branch (parallel) = a motor holding the prestress.
\newcommand{\maxwellelement}[1][1.0]{%
\begin{tikzpicture}[scale=#1,line width=1pt,black!85,every node/.style={font=\small},
    >={Latex[length=2mm]}]
  \def\xL{0}\def\xR{7.2}\def\yT{1.7}\def\yB{0}
  % vertical rails at both ends
  \draw (\xL,\yB)--(\xL,\yT);
  \draw (\xR,\yB)--(\xR,\yT);
  % applied load sigma, pulling outward left & right
  \coordinate (Lmid) at (\xL,{(\yT+\yB)/2});
  \coordinate (Rmid) at (\xR,{(\yT+\yB)/2});
  \draw (Lmid)--++(-0.55,0); \draw[<-] (Lmid)++(-0.5,0)--++(-0.5,0)
    node[left,font=\normalsize]{$\sigma^i$};
  \draw (Rmid)--++(0.55,0);  \draw[->] (Rmid)++(0.5,0)--++(0.5,0)
    node[right,font=\normalsize]{$\sigma^i$};
  % ===== TOP branch: Maxwell arm = dashpot --- spring (in series) =====
  \draw (\xL,\yT)--(0.7,\yT);                          % lead in
  % dashpot: horizontal cylinder (open right) + piston from the right
  \draw (0.7,\yT-0.32)--(0.7,\yT+0.32)--(1.5,\yT+0.32); % cylinder back+top
  \draw (0.7,\yT-0.32)--(1.5,\yT-0.32);                 % cylinder bottom
  \draw[line width=3pt] (1.28,\yT-0.24)--(1.28,\yT+0.24); % piston plate
  \draw (1.28,\yT)--(2.0,\yT);                          % piston rod
  \node[above,font=\footnotesize,align=center] at (1.1,\yT+0.42)
    {$\dfrac{\dot\varrho_0^{\,i}}{\varrho_0^{\,i}}+\dfrac{1}{T^{i}}$ \ (turnover)};
  % spring (horizontal zig-zag) 2.0 -> 4.6
  \draw (2.0,\yT)
    --(2.2,\yT+0.28)--(2.6,\yT-0.28)--(3.0,\yT+0.28)--(3.4,\yT-0.28)
    --(3.8,\yT+0.28)--(4.2,\yT-0.28)--(4.4,\yT)--(4.6,\yT);
  \node[below,font=\footnotesize] at (3.3,\yT-0.34) {$\mathbb{C}^{i}$ (elastic)};
  \draw (4.6,\yT)--(\xR,\yT);                           % lead out
  % ===== BOTTOM branch (parallel): MOTOR holding the prestress =====
  \draw (\xL,\yB)--(3.05,\yB);
  % motor glyph: circle with an M-arc + drive arrow (active element)
  \draw[fill=black!5] (3.6,\yB) circle (0.5);
  \draw[->,line width=0.8pt] (3.35,\yB-0.18) arc (215:-35:0.26);  % rotation arc
  \node[font=\footnotesize] at (3.6,\yB) {M};
  \draw (4.1,\yB)--(\xR,\yB);
  \node[below,font=\footnotesize] at (3.6,\yB-0.66)
    {motor: prestress $\bar\sigma^{i}_{\text{pre}}$};
\end{tikzpicture}}

% ---- constrained mixture: cohorts, each its own natural configuration ------
\newcommand{\cmmmap}[1][1.0]{%
\begin{tikzpicture}[scale=#1,every node/.style={font=\small}]
  % three small stress-free cohort configs (left, stacked)
  \foreach \y/\lab in {1.7/{\tau=0}, 0.0/{\tau}, -1.7/{s}} {
    \begin{scope}[shift={(0,\y)}]
      \draw[refbody] plot[smooth cycle,tension=0.85]
        coordinates {(0,0)(0.55,0.32)(0.95,0.05)(0.82,-0.5)(0.32,-0.62)(-0.06,-0.3)};
      \node[font=\scriptsize,anchor=east] at (-0.15,-0.15) {$\lab$};
    \end{scope}}
  \node[font=\footnotesize,align=center] at (0.45,-2.75)
    {cohorts, each at\\deposition stretch $G^k$};
  % shared current mixture (right)
  \begin{scope}[shift={(5.0,0)}]
    \draw[curbody] plot[smooth cycle,tension=0.9]
      coordinates {(0,0.4)(1.3,1.0)(2.3,0.35)(2.15,-1.1)(1.0,-1.6)(-0.1,-0.85)};
    \node[font=\footnotesize] at (1.05,-2.1) {current mixture $\mathcal{B}_t$};
    \node[font=\scriptsize,align=center,black!70] at (1.05,-0.35)
      {constrained to\\deform together};
  \end{scope}
  % arrows from each cohort into the current mixture
  \draw[cfgmaparrow] (1.0,1.55) to[bend left=10]
    node[above,font=\footnotesize]{$\bm{F}_{n(\tau)}$} (5.0,0.5);
  \draw[cfgmaparrow] (1.0,-0.15) -- (4.9,-0.25);
  \draw[cfgmaparrow] (1.0,-1.85) to[bend right=10] (5.0,-1.15);
\end{tikzpicture}}


% ---- compact coding-exercise field (adds a monospace "run this" line) ------
\newcommand{\qrun}[1]{\par\smallskip{\color{QBlue}\bfseries Run.}\ \texttt{#1}}
