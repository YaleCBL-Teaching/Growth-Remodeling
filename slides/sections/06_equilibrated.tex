\begin{frame}[t]{}
  \vfill\begin{center}{\Huge\color{YaleBlue} Equilibrated CMM}
  \end{center}\vfill
\end{frame}

\begin{frame}[t]{Directly Solve for the End State}
  Set every rate to zero and solve the \textbf{algebraic} equilibrium, one
  root-find instead of thousands of time steps.
  \dlab{the 3D equilibrium conditions}
  \vspace{-0.2em}
  \giveneq{\underbrace{\Upsilon^{\alpha}=1\Rightarrow\bar\sigma^*=\bar\sigma_h}_{\text{(A) growth balance}}
    \quad
    \underbrace{\bm{F}_n^{\alpha}=\mathrm{const}\Rightarrow\sigma^{\alpha}=\sigma_h^{\alpha}}_{\text{(B) remodeling completed}}
    \quad
    \underbrace{\nabla\!\cdot\bm{\sigma}=\bm 0}_{\text{(C) mechanical equilib.}}}
  \zlab{scalar conditions at the evolved geometry}
  \giveneq{\bar\sigma^*=\bar\sigma_h,\qquad \lambda_e^{\alpha}=G^{\alpha},\qquad
    \sigma_\theta=\tfrac{P a}{h}.}
  \srccite{Latorre, Humphrey, ZAMM (2018)}
\end{frame}

\begin{frame}[t]{Collapse to One Scalar Equation}
  Let the turnover constituents grow in fixed proportion (exact when they share
  gain and turnover). The 3D conditions (A)--(C) collapse to a \emph{single}
  equation for the evolved stretch $\lambda^*$, consistent with the same
  $\lambda,\ G^{\alpha},\ \phi^{\alpha}$ used throughout:
  \zlab{scalar equilibrium equation for $\lambda^*$}
  \slboxeq{eq:equil}{\phi^e\,\xi
    + \xi\,\frac{\bar\sigma_h - \sigma^e(G^e\lambda^*)}{\sigma_{\text{turn}} - \sigma_h^e}
    - \gamma\,(\lambda^*)^{2} = 0}
  with the Laplace exponent $n=2$, $\gamma$ the sustained load factor, $\xi$ the
  surviving elastin fraction, $\phi^e$ the elastin mass fraction,
  $\sigma_h^e=\sigma^e(G^e)$ the elastin homeostatic stress, and $\sigma_{\text{turn}}$
  the homeostatic stress carried by the turnover constituents (collagen and smooth
  muscle). Once $\lambda^*$ is known, the evolved mass ratio is $M/M_0=\gamma(\lambda^*)^2$.
  \vspace{0.3em}

  This equation \emph{is} the fixed point of the homogenized ODEs (\eqref{eq:homog_mass}--\eqref{eq:homog_remodel} with $\dd/\dd t=0$), so its root coincides with the long-time limit of the transient theory.
  \srccite{Latorre, Humphrey, ZAMM (2018)}
\end{frame}

\begin{frame}[t]{The Punchline: Existence Sets the Capacity Limit}
  Equation \eqref{eq:equil} may have \textbf{no physical root}. Then no adapted
  state exists and the transient theories dilate without bound. So the solver is
  the cleanest \textbf{existence test}: no root $\Rightarrow$ the wall lacks the
  \emph{capacity} to adapt. Existence is \emph{necessary} for stability; whether the
  transient actually \emph{reaches} an equilibrium that exists is a separate,
  gain-dependent question (dynamic stability). Near the boundary the transient slows
  critically, which is exactly why the instant algebraic test is so useful.
\end{frame}

\begin{frame}[t]{}
  \begin{setupbox}{Example (the equilibrated stability test, \texttt{ex04})}
    \sprob{Skip the transient: solve the single algebraic equation \eqref{eq:equil}
    for the \emph{final} adapted stretch $\lambda^*$. A physical root means the
    tissue adapts; \emph{no root} means it dilates without bound. Scanning elastin
    loss then traces the stability boundary.}
    \srun{\scodeline{uv run python run.py configs/ex04\_equilibrated.yaml}
      Here \texttt{study: equilibrium} solves once for the given insult (and prints
      a transient cross-check: the homogenized plateau lands on $\lambda^*$), then
      the \texttt{scan:} block sweeps \texttt{elastin\_surviving} and
      \texttt{report\_critical: elastin} prints the critical surviving fraction
      $\xi_\text{crit}$.}
    \sstudy{Move the boundary by changing the wall's load-bearing \emph{capacity},
      then re-read $\xi_\text{crit}$:
      \scodeline{... --set model.constituents.collagen.law.c1=600}
      \scodeline{... --set model.constituents.collagen.G=1.15}
      (Raising the \texttt{gain} leaves $\xi_\text{crit}$ unchanged: gain sets how
      \emph{fast} the transient adapts, not \emph{whether} an equilibrium exists.)}
  \end{setupbox}
\end{frame}

\begin{frame}[t]{}
  \begin{yourturnbox}{Equilibrated}
    \qq{Does the algebraic $\lambda^*$ land on the transient plateau where a root exists? What is the critical elastin loss beyond which \emph{no} equilibrium exists at all?}
  \end{yourturnbox}
\end{frame}

\begin{frame}[t]{}
  \begin{answerbox}{Equilibrated}
    Where a root exists, the equilibrated solve lands exactly on the transient end-state. Below $\xi_{\text{crit}}$ there is \emph{no} root: the transient dilates without bound.
  \end{answerbox}
  \vspace{-0.3em}
  \bigfigw{fig04_equilibrated.pdf}{\linewidth}{Your run: equilibrated end-state
  on the transient plateau where a root exists; near the boundary, critical slowing.}
\end{frame}
