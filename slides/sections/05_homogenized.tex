\begin{frame}[t]{}
  \vfill\begin{center}{\Huge\color{YaleBlue} Homogenized CMM}
  \end{center}\vfill
\end{frame}

% \begin{frame}[t]{Homogenize: One Mean Natural Configuration per Constituent}
%   \textbf{Temporal homogenization} gives each constituent a \emph{single, evolving} mean natural configuration. Two ODEs per constituent replace the entire history.
%   \dlab{general form (Cyron 2016): homogenized elastic deformation}
%   \giveneq{\bm{F}^{\alpha}_{e}(s) = \bm{F}(s)\,\big[\bm{F}^{\alpha}_{n}(s)\big]^{-1}}
%   \zlab{0D reduction: scalar mean natural stretch}
%   \giveneq{\lambda_e^{\alpha} = \lambda/\lambda_n^{\alpha}}
%   \begin{center}\homogmap[0.56]\end{center}
%   \srccite{Cyron, Aydin, Humphrey, Biomech Model Mechanobiol (2016)}
% \end{frame}

\begin{frame}[t]{The Mechanical Analog: A Maxwell Fluid with a Motor}
  Each constituent behaves like a \textbf{Maxwell element in parallel with a
  motor}: turnover relaxes stress toward a \emph{prestress}, not zero.
  \begin{center}\maxwellelement[0.78]\end{center}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \footnotesize
      \renewcommand{\arraystretch}{1.5}
      \begin{tabular}{@{}ll@{}}
        $\sigma^{\alpha}$ & constituent stress (the load)\\[2pt]
        $\mathbb{C}^{\alpha}$ & elastic stiffness (spring)\\[2pt]
        $\dot\varrho_0^{\alpha}/\varrho_0^{\alpha}+1/T^{\alpha}$ & turnover + degradation (dashpot)\\[2pt]
        $\bar\sigma^{\alpha}_{\text{pre}}$ & deposition prestress (motor)\\
      \end{tabular}
    \end{column}
    \begin{column}{0.48\textwidth}
      \footnotesize \textbf{Back to the full CMM (0D):}
      \begin{itemize}\itemsep3pt
        \item spring $\leftrightarrow$ constituent law $\sigma^{\alpha}(\lambda_e^{\alpha})$
        \item dashpot $\leftrightarrow$ mass turnover / removal $k_d^{\alpha}$
        \item motor $\leftrightarrow$ deposition stretch $G^{\alpha}$
      \end{itemize}
    \end{column}
  \end{columns}
  \srccite{Cyron, Aydin, Humphrey, Biomech Model Mechanobiol (2016)}
\end{frame}

\begin{frame}[t]{Growth (Mass) and Remodeling (Natural Configuration)}
  Two 0D ODEs per constituent replace the cohort history.\\[1em]
  \begin{columns}[c]
    \begin{column}{0.45\textwidth}
    \textbf{Growth}: mass production $\propto$ mass, driven by the tissue-stress deviation.\\
      \slboxeq{eq:homog_mass}{\frac{\dd M^{\alpha}}{\dd t}
        = M^{\alpha} K_\sigma^{\alpha} k_d^{\alpha}\!\left(\tfrac{\bar\sigma}{\bar\sigma_h}-1\right)}
    \end{column}
    \begin{column}{0.45\textwidth}
      \textbf{Remodeling}: new mass enters at natural stretch $\lambda/G^{\alpha}$, old mass leaves at the
      current $\lambda_n^{\alpha}$ (elastic stretch $\lambda_e^{\alpha}=\lambda/\lambda_n^{\alpha}$).
      \slboxeq{eq:homog_remodel}{\frac{\dd\lambda_n^{\alpha}}{\dd t}
        = \frac{m^{\alpha}}{M^{\alpha}}\!\left(\frac{\lambda}{G^{\alpha}}-\lambda_n^{\alpha}\right)}
    \end{column}
  \end{columns}
  \vspace{0.3em}
  with production $m^{\alpha}=k_d^{\alpha}M^{\alpha}\,\Upsilon^{\alpha}$ and stimulus
  $\Upsilon^{\alpha}=1+K_\sigma^{\alpha}(\bar\sigma/\bar\sigma_h-1)$. Holding
  $\lambda$ fixed, remodeling relaxes each constituent's stress to its prestress
  $\sigma_h^{\alpha}$ (time constant $\sim\!1/k_d^{\alpha}$), the Maxwell-with-motor
  behaviour.
  \srccite{Cyron, Aydin, Humphrey, Biomech Model Mechanobiol (2016)}
\end{frame}

\begin{frame}[t]{}
  \begin{yourturnbox}{Why Is the Homogenized Model Cheap?}
    \qq{How does the cost of the homogenized constrained mixture model scale
    with the number of steps $N$, compared with the full model?}
  \end{yourturnbox}
  \vspace{0.6em}
  \srev
  \begin{answerbox}{Why Is the Homogenized Model Cheap?}
    The state is \emph{fixed-size}, two numbers $(M^{\alpha},\lambda_n^{\alpha})$ per constituent, and each step just advances two ODEs, with \emph{no} history to re-sum:
    \giveneq{\text{homogenized: }\mathcal{O}(N)\qquad\text{vs.}\qquad
      \text{full CMM: }\mathcal{O}(N^2)}
    Same time trajectory, far cheaper, and the fixed state maps onto a viscoelastic (Maxwell-with-motor) material, so it drops traight into standard finite-element solvers.
  \end{answerbox}
\end{frame}

\begin{frame}[t]{}
  \begin{yourturnbox}{When do homogenized and full agree?}
    \qq{The homogenized model keeps only the \emph{mean} natural configuration per
    constituent. Under what assumptions should it match the full deposition-history
    model and when would you expect them to diverge? \qkind{discuss}}
  \end{yourturnbox}
  \vspace{0.6em}
  \srev
  \begin{answerbox}{When do homogenized and full agree?}
    They agree when each constituent's cohorts share \emph{nearly the same} natural
    configuration i.e.\ slow, gradual loading where deposition stretch $G^{\alpha}$ and
    turnover are roughly stationary, so the cohort spread stays narrow and the mean
    represents it well. {\color{ABlue}\bfseries They diverge} under fast or strongly
    time-varying insults, when cohorts deposited at very different states coexist and
    a single mean stretch can no longer capture the distribution.
  \end{answerbox}
\end{frame}

\begin{frame}[t]{}
  \begin{setupbox}{Example (homogenized vs.\ full, \texttt{ex03})}
    \sprob{Run both mixture theories on the \emph{same} artery and insult. How closely does the homogenized model track the full model, and how much faster is it?}
    \srun{\scodeline{uv run python run.py configs/ex03\_homogenized\_vs\_full.yaml}
      Here \texttt{study: compare} overlays \texttt{constrained\_mixture},
      \texttt{homogenized\_cmm} and \texttt{kinematic\_growth} on one scenario,
      and prints each runtime and the mass-ratio gap between the first two (full
      vs.\ homogenized).}
    \sstudy{Stress the comparison with a harsher, longer insult:
      \scodeline{... --set insult.elastin\_surviving=0.3 --set insult.ramp=10}
      \scodeline{... --set simulate.t\_end=4000\ \ \ \ \ \# does the agreement hold?}}
  \end{setupbox}
\end{frame}

\begin{frame}[t]{}
  \begin{yourturnbox}{Homogenized vs.\ full}
    \qq{Is the difference between the two curves visible by eye? By what factor is
    the homogenized model faster and does the agreement survive a harsher,
    longer insult?}
    \qhint{Read the printed mass-ratio gap and the two runtimes. The homogenized
    model should track the full one closely wherever the loading is slow and
    gradual.}
  \end{yourturnbox}
\end{frame}

\begin{frame}[t]{}
  \begin{answerbox}{Homogenized vs.\ full}
    The homogenized curve overlays the full CMM closely at a fraction of the
    runtime ($O(N)$ vs.\ $O(N^2)$). {\color{ABlue}\bfseries Learning:} it reproduces
    a \textbf{similar time trajectory} to the full model, cheaply.
  \end{answerbox}
  \vspace{-0.4em}
  \bigfigw{fig03_compare_all.pdf}{0.52\linewidth}{The full comparison (all theories,
  hypertension + aneurysm). Your \texttt{ex03} run reproduces the hypertension overlay:
  homogenized (dashed) on the full CMM (gold), with kinematic growth (blue) under-responding.}
\end{frame}
